
remove qrcode() and qrencode dependency

remove directory traversal from set_git function, due to flat structure

remove verify_file(), $PASSWORD_STORE_SIGNING_KEY, cmd_extension(), $PASSWORD_STORE_ENABLE_EXTENSIONS, $PASSWORD_STORE_EXTENSIONS_DIR, cmd_extension_or_show(), cmd_copy_move(),

remove PASSWORD_STORE_KEY

remove set_gpg_recipients(), reencrypt_path(), non-linux platform support

remove select line option, cmd_grep()

remove echo, force, and multiline flags from insert, remove 
cmd_generate(), merge generate into insert 

remove clip flag from show and insert(generate), remove yesno, change missing /dev/shm from prompt to warning in tmpdir()

remove set_git()





note: key rotation
